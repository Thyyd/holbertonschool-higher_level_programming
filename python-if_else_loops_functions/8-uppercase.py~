#!/usr/bin/python3
def uppercase(strg):
    renv = ""
    for car in range(len(strg)):
	if ord('a') <= ord(strg[car]) <= ord('z'):
	    att = ord(strg[car]) - 32
	    renv += chr(att)
	else:
	    renv += strg[car]
    return renv
